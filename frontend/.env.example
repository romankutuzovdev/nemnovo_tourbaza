# URL бэкенда (без слэша в конце). Нужен для запросов переводов и услуг.
# Для ngrok: оставьте пустым (запросы пойдут через прокси Next.js).
NEXT_PUBLIC_API_URL="https://carries-reg-careful-mix.trycloudflare.com"

# Для ngrok: URL бэкенда для server-side rewrites.
# BACKEND_URL=http://127.0.0.1:8000
